[project]
name = "us-economy-agent"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "snowflake-snowpark-python>=1.53.1",
    "streamlit>=1.59.2",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]

